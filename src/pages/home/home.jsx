import { Card, Layout } from 'antd';
import React from 'react';

const { Header, Content, Footer, Sider } = Layout;

const Home = () => {
    return (
        <Card>
            the home1
        </Card>
    )
}

export default Home;